//use crate::geometry::*;
//use crate::*;

/*
pub struct BVH<T: Intersectable + Bounded> {
    pub objects: Vec<T>
}

impl<T: Intersectable + Bounded> Intersectable for BVH<T> {
    fn intersect(&self, ray: &Ray, tmin: f32, tmax: f32) -> Option<Hit> {
        todo!()
    }
}
*/
